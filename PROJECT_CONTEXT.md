# PROJECT_CONTEXT.md

本文档记录 Lumina 项目的背景、当前开发状态、系统设计思路和后续方向。它比 `README.md` 更详细，适合作为继续开发、写课程汇报、让 AI 理解项目上下文时的参考。

## 1. 项目背景

Lumina 当前服务于“交互游戏设计”方向的课程作业和原型开发。作业设计对象是学龄前儿童，设计目标是提高儿童的社交技能，例如：

- 情感理解
- 自我需求表达
- 对他人意图的理解
- 轮流、等待、分享
- 主动邀请同伴共同游戏
- 基础沟通技巧

项目目前的核心想法是：通过一个可交互的 3D 场景，让儿童在接近 NPC、选择回应、观察 NPC 反馈、完成动作任务的过程中练习社交行为。后续可以把摄像头姿态识别接入为无穿戴交互方式，让儿童不用手柄或穿戴设备，也能通过站位、挥手、举手等身体动作参与游戏。

## 2. 当前设计方向

### 2.1 交互设备

当前建议采用“无穿戴视觉捕捉 + 屏幕反馈”的方式：

- 输入设备：普通摄像头。
- 识别方式：Python + MediaPipe Pose。
- 输出设备：电脑屏幕或投影。
- 游戏引擎：Unity。
- 辅助输入：开发阶段仍可保留键盘、鼠标、Unity Input System，便于调试。

这个方向适合学龄前儿童，因为它降低了设备佩戴和操作复杂度。儿童只需要站到指定位置、做出简单动作或选择表达内容，就能触发游戏反馈。

### 2.2 游戏目标

游戏不追求复杂操作，而是强调清晰、短时、可重复的社交情境练习：

- 看到同伴或 NPC 的状态。
- 判断对方可能想做什么。
- 选择合适的回应。
- 用动作或语言表达自己的需求。
- 等待反馈并观察结果。

比较适合的场景包括：

- 邀请同伴一起玩。
- 排队等待轮到自己。
- 分享玩具或材料。
- 向老师或同伴表达需求。
- 识别对方开心、难过、犹豫、需要帮助等状态。

## 3. 当前技术结构

### 3.1 Unity 项目环境

- Unity 版本：`2022.3.62f3c1`
- 渲染管线：URP
- 主要依赖：Input System、Cinemachine、Timeline、Animation Rigging、TextMeshPro
- 姿态识别工具：`Tools/PosePython`

### 3.2 目录结构

- `Assets/Scripts`
  - NPC 互动、对话 UI、事件系统、玩家状态控制。

- `Assets/Test`
  - MediaPipe 姿态捕捉测试、UDP/管道通信、Avatar 骨骼映射、校准脚本。

- `Assets/Dialogues`
  - `DialogueNode` 对话配置资源。

- `Assets/Scenes`
  - 游戏场景和社交情境场景。

- `Assets/Editor`
  - 编辑器扩展脚本，例如 Shader 动画控制相关 Inspector。

- `Tools/PosePython`
  - Python 摄像头读取、MediaPipe Pose 检测和数据发送。

## 4. NPC 社交互动系统

NPC 互动系统的目标是形成一个稳定的游戏闭环：

```text
玩家靠近触发区域
-> 打开社交选项 UI
-> 玩家选择一个回应
-> NPC 或场景播放反馈
-> 关闭 UI
-> 玩家回到 3D 操作模式
```

### 4.1 DialogueNode

`Assets/Scripts/DialogueNode.cs` 使用 ScriptableObject 配置对话节点和选项。

当前对话选项可以表达不同结果：

- 跳转到下一个对话节点。
- 结束对话并关闭 UI。
- 播放 Timeline。
- 让 NPC 面向玩家并播放指定动画。

设计思路是让剧情和交互内容尽量数据化。后续新增社交场景时，优先通过新建或调整 `DialogueNode` 资源完成，而不是为每个 NPC 单独写一套逻辑。

### 4.2 NPCInteractionTrigger

`Assets/Scripts/NPCInteractionTrigger.cs` 负责玩家进入触发范围后的交互启动。

当前设计允许“触发器”和“实际 NPC”分离。例如排队空位、地面提示圈、桌边区域都可以作为触发器，但真正转向和播放动画的对象可以是旁边的 NPC。

这个设计对儿童游戏很有用，因为儿童注意到的往往不是 NPC 身上的碰撞体，而是地上的空位、发光区域、任务物体或提示圈。

### 4.3 SocialUIManager

`Assets/Scripts/SocialUIManager.cs` 负责选项 UI 的显示和点击处理。

当前需要重点保证：

- UI 显示时鼠标可见并解锁。
- UI 关闭后鼠标状态恢复，玩家可以继续 3D 操作。
- 点击 End 类型选项后关闭 UI。
- 点击 NPC 动画类型选项后，NPC 面向玩家、播放动画、结束后复位。

### 4.4 NPC 动画反馈

Timeline 适合复杂演出，但制作成本较高。当前已经新增轻量的 NPC 动画结果类型，用来处理常见反馈：

- NPC 面向玩家。
- 播放一个配置好的 Animator State。
- 等待指定持续时间。
- 恢复 NPC 原始朝向。
- 恢复 NPC 原始位置。

这种方式适合大量重复的小反馈，例如点头、挥手、欢迎、拒绝、等待、鼓励等。

## 5. MediaPipe 无穿戴视觉捕捉系统

### 5.1 当前管线

当前视觉捕捉管线大致为：

```text
摄像头
-> Python/OpenCV 读取画面
-> MediaPipe Pose 检测人体关键点
-> UDP 或管道发送数据
-> Unity 接收关键点
-> Avatar 骨骼映射和动作更新
```

主要文件：

- `Tools/PosePython/main.py`
- `Tools/PosePython/body.py`
- `Tools/PosePython/global_vars.py`
- `Assets/Test/PosePythonProcess.cs`
- `Assets/Test/ServerUDP.cs`
- `Assets/Test/PipeServer.cs`
- `Assets/Test/Avatar.cs`
- `Assets/Test/AvatarCalibrationHotkey.cs`

### 5.2 帧率关系

摄像头、MediaPipe 和 Unity 不需要完全同步：

- 摄像头常见约 30 FPS。
- MediaPipe 检测通常约 20-30 FPS。
- Unity 可以继续以 60 FPS 运行。

当 Unity 暂时没有收到新的姿态数据时，可以继续使用上一帧目标，并通过平滑移动保持动作连续。所以“Unity 60 FPS、姿态检测 20-30 FPS”是正常组合。

### 5.3 未来接入方式

不要让游戏逻辑直接依赖原始人体关键点。更推荐增加一个中间层，把姿态识别结果转换成游戏语义，例如：

- 挥手：邀请同伴。
- 站到发光空位：排队等待。
- 举手：表达需求或请求帮助。
- 双手靠近胸前：表达想要。
- 面向 NPC 并停留：倾听或关注。

这个中间层可以暂称为 `SocialIntent`。它的作用是把“身体动作”转成“社交行为”，再交给 NPC 互动系统或任务系统处理。

## 6. 提示特效与视觉反馈

当前项目中已经有提示类 Shader 动画控制思路，相关文件包括：

- `Assets/Scripts/PlayModeOnlyShaderAnimation.cs`
- `Assets/Editor/PlayModeOnlyShaderAnimationEditor.cs`

目标是让类似 `Xradiation`、`QueueSlot_AJOutline` 的提示效果在编辑器下保持静止，只有进入 Play Mode 后才开始闪烁或流动。

这种提示适合儿童游戏，因为它可以降低理解成本：

- 哪个位置可以站。
- 哪个 NPC 可以互动。
- 哪个空位需要排队。
- 当前目标在哪里。

视觉提示应尽量柔和、明确，不要过度闪烁，避免造成干扰。

## 7. 已形成的开发决策

### 7.1 先稳定交互闭环，再扩大内容

目前最重要的是确保一条 NPC 互动流程稳定运行。只要“靠近、显示、选择、反馈、关闭、恢复控制”稳定，后续就可以复制到更多场景。

### 7.2 简单反馈优先不用 Timeline

Timeline 表现力强，但配置成本高。常见 NPC 反馈优先用可配置动画结果完成，复杂演出再使用 Timeline。

### 7.3 触发器和 NPC 目标分离

触发器可以是地面区域、空位、物体或任意 Collider，不必一定在 NPC 身上。实际播放动画或转向的 NPC 通过配置指定。

### 7.4 NPC 反馈后要复位

NPC 播放动画后应恢复触发前的位置和朝向。这样可以避免 Root Motion 或动画位移导致 NPC 偏离原本位置。

### 7.5 姿态识别和社交逻辑分层

MediaPipe 提供的是身体关键点，不等于游戏行为。后续应该把姿态识别结果先转成社交意图，再驱动对话、任务或 NPC 反馈。

## 8. 面向课程汇报的表达方式

如果把当前项目整理成 3-5 分钟 PPT，可以按下面结构讲：

1. 设计对象和目标
   - 学龄前儿童。
   - 提高情感理解、自我表达、轮流等待和沟通能力。

2. 交互设备
   - 普通摄像头。
   - MediaPipe 无穿戴视觉捕捉。
   - Unity 3D 场景反馈。

3. 游戏玩法
   - 儿童进入社交场景。
   - 靠近 NPC 或站到提示区域。
   - 选择回应或做出动作。
   - NPC 给出表情、动作或语言反馈。

4. 界面设计
   - 选项按钮要少、文字清晰、反馈明确。
   - 可交互位置使用柔和发光轮廓提示。
   - UI 出现时暂停或弱化 3D 操作，避免儿童分心。

5. 交互流程示例
   - 看到同伴一个人在玩。
   - 站到同伴附近。
   - 选择“我可以一起玩吗？”
   - NPC 转向玩家并做出欢迎动画。
   - 游戏给予正向反馈。

6. 后续改进
   - 增加动作识别输入。
   - 增加更多社交情境。
   - 增加儿童友好的引导和鼓励反馈。

## 9. 后续开发建议

- 增加 `SocialIntent` 中间层，把姿态输入转成社交语义。
- 为每个社交场景整理教学目标，例如邀请、等待、分享、表达需求。
- 建立 NPC 互动配置检查清单，减少漏配触发器、对话节点、动画状态的情况。
- 给 MediaPipe 校准流程增加更清晰的 UI 提示。
- 把简单 NPC 动画反馈做成可复用配置，减少 Timeline 制作压力。
- 在新增场景前先稳定一个完整样板场景，后续按样板复制。

## 10. 修改项目时的提醒

- 修改代码前先阅读 `AGENTS.md`。
- 修改场景或 ScriptableObject 前先查看 Git 状态。
- 不要清理 Unity 生成目录。
- 不要随意删除 `.meta` 文件。
- 改完 NPC 交互后测试鼠标状态、UI 状态和玩家控制是否恢复。
- 改完 MediaPipe 后测试 Python 进程、摄像头输入和 Unity 接收数据。
